let speed;

function setup() {
    createCanvas(600, 400);
    background(0, 0, 28000000000000000000000000)
}

function draw() {
    noStroke();
     fill(280, 0, 0);
    circle(width/2, height/2, 40);
}

